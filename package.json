{
  "name": "vite-project",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite preview --port 8081",
    "build": "tsc && vite build",
    "serve": "vite preview --port 8080",
    "build-serve": "tsc && vite build && vite preview --port 8080"
  },
  "devDependencies": {
    "@types/dat.gui": "^0.7.6",
    "@types/three": "^0.128.0",
    "typescript": "^4.2.3",
    "vite": "^2.3.4"
  },
  "dependencies": {
    "cannon-es-control": "^0.0.4",
    "dat.gui": "latest",
    "three": "^0.129.0",
    "socket.io-client": "^4.4.0"
  }
}
